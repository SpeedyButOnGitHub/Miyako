// Startup summary feature removed; keep a minimal test placeholder so the suite
// remains stable until test suites are reconciled with the removal.
test('startup summary feature removed - placeholder', () => {
  expect(true).toBe(true);
});
